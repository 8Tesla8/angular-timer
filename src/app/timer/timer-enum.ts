export enum TimerVisualOption{
    Full = "full",
    Small = "small"
  }
  
  export enum TimerOption{
    None = "none",  
    ShowFixedTime = "fixed",
    CountDown = "countDown",
    Clear = "clear",
  }